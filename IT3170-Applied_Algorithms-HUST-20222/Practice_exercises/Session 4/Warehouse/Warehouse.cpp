#include <bits/stdc++.h>
#define MAX 1000
using namespace std;

int n, T, D;
int a[MAX], t[MAX], f[MAX][MAX];

void input() {
	cin >> n >> T >> D;
	
	for (int i = 0; i < n; i++) cin >> a[i];
	for (int i = 0; i < n; i++) cin >> t[i];
}

int main() {
	input();
	
	for (int i = 0; i < n; i++) {
		
	}
	return 0;
}
